'use client';

// Temporary placeholder during Kakao Maps migration.
// Real implementation lands in Task 2.
export default function MapContainer() {
  return (
    <div className="flex h-full items-center justify-center bg-gray-50 text-gray-500">
      지도 마이그레이션 진행 중 (Task 2에서 완성됩니다)
    </div>
  );
}
